{
  "permissions": {
    "allow": [
      "Bash(git init *)",
      "Bash(git add *)",
      "Bash(git config *)",
      "Bash(git commit -m 'Fix: Prevent users from losing progress on browser refresh + Auto-save session every 10 seconds *)"
    ]
  }
}
